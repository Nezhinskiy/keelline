`keelline init --questions` prints each value `keelline init --yes` would take, where it came
from and the flag that replaces it, and under `--json` carries the six as a JSON Schema. It
writes nothing.
