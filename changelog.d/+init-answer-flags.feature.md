`keelline init --yes` takes `--name`, `--base-branch`, `--agent`, `--profile`,
`--memory-mode` and `--local`, each replacing one default `keelline init --questions` shows
and each held to its grammar or its choices by the parser. `--local` offers the four files a
project may keep out of git with every gate still passing.
