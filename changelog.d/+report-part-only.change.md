A plan report now says when a removal takes only Keelline's section out of a file that holds
other text: the line reads `(retired; Keelline's part only, the file stays)`, so it no longer
looks like the whole file being deleted.
