`keelline init` delivers a stack profile to every harness a project names: its rules land
once, in `docs/keelline/rules/`, as the one copy the project edits; every harness reading
`AGENTS.md` is handed a pointer to it and the profile's must-know lines; and Claude Code also
gets a path-scoped pointer under `.claude/rules/`.
