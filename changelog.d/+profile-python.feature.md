The first stack profile, `python`: recommended defaults every agent is handed, and eight
static checks for `keelline assess` (ships later) to run — a lockfile committed or
deliberately ignored, `requires-python` declared without a cap, one type checker configured,
strict pytest markers in whichever file pytest reads, marker selection kept out of `addopts`,
and Ruff's defaults not hidden by a legacy `select`.
