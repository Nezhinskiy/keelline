# Runbook: bug reports

How to file, close, reference and merge an entry in this project's ledger. The index links
here; the entry files under `docs/bugs/` are the ledger itself.

File with `keelline bugs new`, close by setting the entry's status and `fixed_in`, and
regenerate the index with `keelline bugs index` rather than editing it.
