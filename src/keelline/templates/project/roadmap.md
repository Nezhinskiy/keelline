# Roadmap

Phase status in prose, oldest phase first, with the trail below generated. Closed phases move
to the roadmap history; `keelline docs check` budgets the prose above the trail marker.

## Now

Nothing yet — name the phase this project is in, and what closing it means.

## Next

Nothing yet.

## Design and plan trail

Every design and plan document, grouped by theme and annotated with its
delivery state. Regenerate with `keelline docs trail` after adding a document.
Delivered is the default; anything else is declared in `trail.toml` beside this
file, which mirrors the forward track above.

<!-- generated by keelline docs trail — 0 documents, 0 not plainly delivered -->
<!-- end design and plan trail -->
