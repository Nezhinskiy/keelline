"""Path containment for keelline.toml (contract C1, part three). Task 5 writes the real check."""

from __future__ import annotations


def validate_paths(config: object, root: object) -> dict[str, object]:
    return {}
